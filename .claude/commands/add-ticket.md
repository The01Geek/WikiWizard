To do:
1. First use the `doc-verifier` subagent to document the current state of the features/functionalities as it relates to the user story. Make sure to commit and push the changes.
2. Then pass the findings from the `doc-verifier` subagent to the `github-issue-creator` subagent to create a GitHub issue based on the user story and the documentation.
3. After the issue is created, remove the `requirements.md` temporary file created by the `github-issue-creator` subagent.

User Story (rough draft): $ARGUMENTS